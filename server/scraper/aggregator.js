// const scrapeBooking = require('./booking');
// const scrapeMMT = require('./makemytrip');

// async function getAllHotelLinks(hotelName, location) {
//   const results = await Promise.all([
//     scrapeBooking(hotelName, location),
//     scrapeMMT(hotelName, location),
//     // Add more scrapers
//   ]);

//   return results.filter(r => r.link); // Only valid ones
// }

// module.exports = getAllHotelLinks;

